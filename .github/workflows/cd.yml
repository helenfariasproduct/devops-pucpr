name: CD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar codigo
        uses: actions/checkout@v4

      - name: Simular deploy do Assistente de FAQ
        run: |
          echo "Iniciando deploy do Assistente de FAQ"
          echo "Aplicacao pronta para publicacao"
          echo "Deploy concluido com sucesso"